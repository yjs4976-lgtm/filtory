import { AdminAppShell } from "./AdminAppShell"
import { AdminGuard } from "./AdminGuard"
export function AdminSimplePage({title,description,summary,headers,rows}:{title:string;description:string;summary:Array<[string,string]>;headers:string[];rows:string[][]}){return <AdminAppShell title={title}><AdminGuard><div className="admin-heading"><h2>{title}</h2><p>{description}</p></div><div className="admin-compact-kpis">{summary.map(([label,value])=><article key={label}><span>{label}</span><strong>{value}</strong></article>)}</div><section className="admin-panel"><div className="admin-table-wrap"><table className="admin-data-table"><thead><tr>{headers.map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i}>{row.map((cell,j)=><td key={j}>{cell}</td>)}</tr>)}</tbody></table></div></section></AdminGuard></AdminAppShell>}
